namespace ConsoleApp9
{
    internal class init
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Hello, World!");
       class Punt :
    static __init__(self, x, y);
        self x = x
        self y = y

    static mostrar_coordenades(self):
        init("Les coordenades del punt són: ({self.x}, {self.y})");

      # Creació del punt p1
      p1 = init(3, 4)

      # Mostra les coordenades del punt p1
      p1.mostrar_coordenades();
    }
    }
}
